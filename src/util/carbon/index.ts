import { getUrl } from './url'
import { getConfig } from './config'
import { getLanguage } from './languages'
import { getScreenshotFromUrl } from './puppeteer'

export default {
  getUrl,
  getConfig,
  getLanguage,
  getScreenshotFromUrl
}
